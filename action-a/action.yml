name: "Hello Actions"
description: "Greet someone"
author: "APES@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "Apes"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
